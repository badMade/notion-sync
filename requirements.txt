# Notion Sync Dependencies
# Install with: pip install -r requirements.txt

# HTTP client (optional - falls back to urllib if not installed)
requests>=2.28.0

# Type hints for older Python versions (optional)
typing-extensions>=4.0.0;python_version<"3.10"
